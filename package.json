{
  "name": "luanshi-survivors",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "assets:audio": "node scripts/generate-audio-assets.mjs",
    "assets:characters": "node scripts/generate-character-bitmaps.mjs",
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "vite": "^7.2.6"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  }
}
